postgres:
    container_name: postgresql
    image: postgres:9.5
    volumes:
        - ./data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
          - POSTGRES_PASSWORD=postgres
